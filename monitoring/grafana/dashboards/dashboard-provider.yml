# Grafana Dashboard Provider Configuration
apiVersion: 1

providers:
  - name: 'HPA Platform Dashboards'
    orgId: 1
    folder: 'HPA Platform'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards